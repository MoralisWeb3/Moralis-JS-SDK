{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "npx lerna run build",
    "release": "npm publish",
    "bootstrap": "npx lerna bootstrap"
  }
}
