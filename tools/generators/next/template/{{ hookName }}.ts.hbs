import { SWRConfiguration } from 'swr/dist/types';
import { T{{ targetMethod }}Params, T{{ targetMethod }}Return } from './types'
import axios from 'axios'
import useSWR from 'swr';

export const {{ hookName }} = (params: T{{ targetMethod }}Params, SWRConfig?: SWRConfiguration) => {
  const axiosFetcher = async (endpoint: string, params: any) => axios.get(`/api${endpoint}`, params).then(res => res.data);

  const { data, error, mutate, isValidating } = useSWR<T{{ targetMethod }}Return['result']>(
    [`/moralis/EvmApi/account/getNativeBalance`, params],
    axiosFetcher,
    SWRConfig,
  );

  return {
    data,
    error,
    refetch: async () => mutate(),
    isValidating,
  };
};
