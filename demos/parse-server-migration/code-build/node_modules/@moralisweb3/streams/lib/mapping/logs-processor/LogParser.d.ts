import { BigNumber } from 'ethers';
import { AbiItem, Log } from '@moralisweb3/streams-typings';
export interface ParsedLog {
    name: string;
    params: Record<string, LogParam>;
}
export interface LogParam {
    value: LogParamValue;
    type: string;
}
export declare type LogParamValue = BigNumber | string;
export declare class LogParser {
    private readonly abiInterface;
    constructor(abiItems: AbiItem[]);
    read(log: Log): ParsedLog;
}
//# sourceMappingURL=LogParser.d.ts.map