{
  "name": "test-node",
  "author": "Moralis",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "build-start": "yarn build && yarn start",
    "start": "node build/main.js",
    "format": "prettier . \"src/**/*.+(js|ts|json)\" --write",
    "format:check": "prettier . \"src/**/*.+(js|ts|json)\" --check"
  },
  "dependencies": {
    "moralis": "^2.0.0-beta.9",
    "dotenv": "^16.0.1"
  }
}
