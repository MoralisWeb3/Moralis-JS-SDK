import Head from 'next/head';

const Meta = () => {
  return (
    <Head>
      <title><%- name %></title>
      <meta name="description" content="<%- name %>" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default Meta;
