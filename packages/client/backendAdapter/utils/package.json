{
  "name": "@moralisweb3/client-backend-adapter-utils",
  "author": "Moralis",
  "version": "2.9.0",
  "license": "MIT",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "sideEffects": false,
  "moralis": {
    "name": "Api Utilities for client-side",
    "type": "utils",
    "scope": "client",
    "description": "Client-side utilities for api calls"
  },
  "files": [
    "lib/*"
  ],
  "scripts": {
    "test": "yarn jest --runInBand --detectOpenHandles --forceExit --ci",
    "test:watch": "yarn run test --watch",
    "lint": "eslint . --ext .js,.ts,.tsx,jsx",
    "clean": "rm -rf lib && rm -rf tsconfig.tsbuildinfo && rm -rf ./node_modules/.cache/nx",
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "jest": "29.3.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@moralisweb3/common-core": "^2.9.0"
  }
}
