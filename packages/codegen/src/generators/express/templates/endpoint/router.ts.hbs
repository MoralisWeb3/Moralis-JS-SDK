import { Router } from 'express';
import { 
  // Resolvers import
} from './resolvers';

export class {{ name }} {
  private apiKey: string;

  constructor(apiKey: string) {
    this.apiKey = apiKey;
  }

  public getRouter() {
    const router = Router();

    // Routes
  
    return router;
  }
}
