/**
 * @warn This file is auto-generated
 * Don't modify it
 */
import Moralis from 'moralis'
import {
  {{#each hooks}}
    {{ this.operation }},
    {{ this.response }},
    {{#if this.request}}{{ this.request }},{{/if}}
  {{/each}}
} from '{{ commonUtils }}';
import { {{#each resolversToImport}}{{ this }},{{/each}} } from '../../resolvers';
import { QueryOptions } from '../../useQuery';

const { baseUrl } = Moralis.{{ module }};

{{#each hooks}}
export const {{ this.name }} = (params?: QueryOptions<{{ this.response }}, Error> & {{ this.request }}) => {
  return {{ this.resolverType }}({{ this.operation }}, baseUrl, params);
};
{{/each}}
